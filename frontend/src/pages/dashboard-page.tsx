import { PageShell } from '@/components/page-shell'

export function DashboardPage() {
  return (
    <PageShell title="Dashboard" description="Cluster and homelab status overview.">
      <p className="text-sm text-muted-foreground">Dashboard widgets will be added in the next tasks.</p>
    </PageShell>
  )
}
